# Dev fallback only. In the app, keys are entered in Settings and stored encrypted by Windows.
# Keys set in the app win over these. Never commit .env.
DEEPSEEK_API_KEY=
PEXELS_API_KEY=
UNSPLASH_ACCESS_KEY=
